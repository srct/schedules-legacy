image: node:latest

types:
  - test

test_Schedules:
    type: test
    before_script:
        - apt-get update -qy -o=Dpkg::Use-Pty=0
        - apt-get install -qq -o=Dpkg::Use-Pty=0 sqlite3
        - npm install --silent
    script:
        - npm test
