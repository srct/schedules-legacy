image: node:latest

types:
  - test

test_Schedules:
    type: test
    before_script:
        - apt-get update -qy
        - apt-get install sqlite3
        - npm config set loglevel warn
        - npm install --quiet
    script:
        - npm test
